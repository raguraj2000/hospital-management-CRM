node_modules/
dist/
data/
*.db
*.db-wal
*.db-shm
apps/client/src-tauri/target/

# Pendrive build output, downloaded installers, and safety copies of the
# clinic's database (real patient data) -- never commit any of it.
handoff/
*.log
.env
.env.*
